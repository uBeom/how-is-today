// let jsonData;
// const data = await res.text();
// const parser = new xml2js.Parser();
// parser.parseString(data, (err, result) => {
//   if (err) throw new Error().message;

//   // jsonData = body[0].items[0].item;
//   jsonData = result;
// });

// return jsonData as unknown as IUseCovidStatus[];
